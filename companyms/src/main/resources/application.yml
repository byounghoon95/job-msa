spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/company
    username: hoon
    password: hoon
  application:
    name: company-service

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create-drop
    database: postgresql

server:
  port: 8081